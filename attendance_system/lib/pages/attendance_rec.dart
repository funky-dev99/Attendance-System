import 'package:flutter/material.dart';

class AttendRec extends StatelessWidget {
  const AttendRec({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
